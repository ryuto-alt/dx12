#include "core/Version.h"

// バージョン定数の唯一の実体。宣言・注意書きは Version.h を参照。
namespace dx12e
{
const char* const kEngineVersion = "1.12.7";

const char* const kUpdateRepoOwner = "ryuto-alt";
const char* const kUpdateRepoName  = "dx12";

const char* const kWhatsNewTitle = "DX12 Engine v1.12.7 の更新内容";

const char* const kWhatsNewBody =
    "v1.12.7: 当たり判定が正しく見えるようになり、シェーダーが用途で仕分けされます\n"
    "\n"
    "■【修正】当たり判定の表示が実際とズレていました\n"
    "  当たり判定を線で表示する機能は前からありましたが、描いていた線が\n"
    "  実際の判定と違っていました。線は出ているので、見ただけでは気づけません。\n"
    "\n"
    "  無視されていたもの:\n"
    "    ・Transform の拡大縮小 … 一番よく効きます。ボックスを置いて scale で\n"
    "      大きくした床は、線だけが元の大きさのままでした\n"
    "    ・コライダーのオフセット … ずらして付けた判定が、ずれていない位置に出ていました\n"
    "    ・親子関係 … 親に付けた判定が、まったく別の場所に線だけ出ていました\n"
    "\n"
    "  判定を作る側と線を描く側が同じ計算を使うようにしたので、\n"
    "  これからは【線からはみ出したら当たらない / 線の中なら当たる】で読めます。\n"
    "\n"
    "■【新機能】トリガーの範囲も表示されます\n"
    "  Trigger の反応範囲を紫の線で出します。入っている間は白く光ります。\n"
    "  「入ったのに発火しない」を調べるとき、プレイヤーとトリガー範囲の\n"
    "  位置関係がその場で見えます。物理の判定とは別々に ON/OFF できます。\n"
    "\n"
    "■【新機能】「当たらない判定」が赤で分かります\n"
    "  コライダーは付いているのに RigidBody が無いものを赤い線で出します。\n"
    "  「コライダーを付けたのにすり抜ける」の一番多い原因がこれです。\n"
    "  今までは線が出ないので、判定が無いのか位置がずれているのか区別できませんでした。\n"
    "\n"
    "  設定に色の凡例も出しました。チェックボックスは「当たり判定を表示」です。\n"
    "\n"
    "■【新機能】シェーダーが用途で自動的に仕分けされます\n"
    "  シェーダーには「メッシュ用」「スプライト用」「画面用」の 3 種類があり、\n"
    "  種類が違うものは割り当てても必ず失敗します（頂点の形が別物のため）。\n"
    "  これをシェーダーの中身から自動で見分けて、Inspector の選択欄に\n"
    "  **合わないものを出さない**ようにしました。\n"
    "  選べてしまってから赤いエラーが出る、が無くなります。\n"
    "  ★中身から判定するので、フォルダを移しても名前を変えてもズレません。\n"
    "\n"
    "■【新機能】シェーダーの選択欄がグループに分かれます\n"
    "  選択欄の中が見出しでまとまります。見出しの決まり方:\n"
    "    1. ソースに `// @group イベント演出` と書いてあればその名前\n"
    "    2. 無ければフォルダ名（assets/shaders/ はフォルダごと読み込みます）\n"
    "    3. どちらも無ければ「その他」\n"
    "  ★何も書かなくてもフォルダで分かれるので、まずはフォルダを切るだけで十分です。\n"
    "\n"
    "■ 既存プロジェクトへの影響\n"
    "  ありません。当たり判定そのものの計算は変わっていません（表示が実際に合っただけです）。\n";
} // namespace dx12e
