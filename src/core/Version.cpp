#include "core/Version.h"

// バージョン定数の唯一の実体。宣言・注意書きは Version.h を参照。
namespace dx12e
{
const char* const    kEngineName  = "Uno Engine";
const wchar_t* const kEngineNameW = L"Uno Engine";

const char* const kEngineVersion = "1.13.0";

const char* const kUpdateRepoOwner = "ryuto-alt";
const char* const kUpdateRepoName  = "dx12";

const char* const kWhatsNewTitle = "Uno Engine v1.13.0 の更新内容";

const char* const kWhatsNewBody =
    "v1.13.0: シーンが速く開くようになりました / 水と海が作れます / 名前が Uno Engine になりました\n"
    "\n"
    "■【高速化】シーンを開くのが 49 秒 → 0.8 秒\n"
    "  ブランチを切り替えた後にシーンを開くと数十秒固まっていた問題を直しました。\n"
    "  原因は 2 つで、どちらも「同じテクスチャを何度も圧縮し直していた」ことでした。\n"
    "\n"
    "    1. 圧縮キャッシュの鍵に、ファイルの更新時刻が入っていました。\n"
    "       git は checkout したファイルの更新時刻を書き換えるので、中身が\n"
    "       1 バイトも変わっていなくてもキャッシュが全部外れていました。\n"
    "       鍵をファイルの中身そのものに変えました。\n"
    "\n"
    "    2. モデルが参照するテクスチャのパスに \"..\" が残っていたため、同じ画像でも\n"
    "       モデルの置き場所ごとに別物として扱われ、置き場所の数だけ圧縮していました。\n"
    "       （VRAM も同じ画像を重複して持っていました）\n"
    "\n"
    "  さらに、プロジェクトを開いた後に全テクスチャをバックグラウンドで圧縮するように\n"
    "  しました。優先度を下げてあるので、その間も編集の邪魔になりません。\n"
    "  一周し終われば、どのシーンを開いても、ブランチを切り替えても待ちが出ません。\n"
    "\n"
    "■【改善】ローディングに「％」と「残り時間」が出ます\n"
    "  512KB を超える大きいシーンだけ、進捗が出ずにバーが往復するだけでした。\n"
    "  つまり一番待たされるシーンに限って、動いているのか固まったのか\n"
    "  分からない状態でした。％・件数・処理中のファイル名・経過秒数を出します。\n"
    "\n"
    "■【新機能】水と海のカスタムシェーダー\n"
    "  ツール（または MCP の create_shader）から、水と海の雛形を作れます。\n"
    "  波の大きさ・細かさ・速さ・濁り・泡の量を Inspector から調整できます。\n"
    "\n"
    "    ★平面を置くときは「分割数」を上げてください（64 以上が目安）。\n"
    "      既定の 4 頂点のままだと、頂点が無いので波が一切出ません。\n"
    "\n"
    "  あわせて、自作シェーダー用の共通ヘッダを同梱しました。\n"
    "      #include \"UnoCustom.hlsli\"\n"
    "  と 1 行書くだけで、時間・カメラ位置・光・ノイズ・波の関数が使えます。\n"
    "  これまでは定数の宣言を手で書き写す必要があり、順番を 1 つ間違えると\n"
    "  エラーも出ずに値だけが化けていました。\n"
    "\n"
    "■【変更】VSync が既定で ON になりました\n"
    "  これまで上限 144fps で描いていたため、60Hz の画面では表示されないフレームを\n"
    "  2 倍以上描いて、ノート PC では発熱でむしろ遅くなっていました。\n"
    "  設定で明示的に OFF にしている場合はそのままです。\n"
    "\n"
    "■【変更】名前が Uno Engine になりました\n"
    "  表示だけの変更です。プロジェクトも設定も、そのまま使えます。\n"
    "\n"
    "■ 既存プロジェクトへの影響\n"
    "  ありません。初回起動時だけ、テクスチャの圧縮キャッシュを作り直します\n"
    "  （バックグラウンドで進みます）。\n";
} // namespace dx12e
