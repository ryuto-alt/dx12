#include "core/Version.h"

// バージョン定数の唯一の実体。宣言・注意書きは Version.h を参照。
namespace dx12e
{
const char* const kEngineVersion = "1.11.0";

const char* const kUpdateRepoOwner = "ryuto-alt";
const char* const kUpdateRepoName  = "dx12";

const char* const kWhatsNewTitle = "DX12 Engine v1.11.0 の更新内容";

const char* const kWhatsNewBody =
    "v1.11.0: 透明マテリアル(葉・フェンス・ガラス) + アセットのホットリロード + 被写界深度の作り直し\n"
    "\n"
    "■【新機能】透明マテリアル(アルファクリップ / アルファブレンド)\n"
    "  今まで葉・枝・フェンス・角膜・ガラスが【全部ただの板】になっていました。\n"
    "  glTF の alphaMode をそのまま読むようにしたので、Blender で Alpha Clip /\n"
    "  Alpha Blend にして書き出せば、そのまま抜けて・そのまま透けます。\n"
    "  ・アルファクリップ(Mask): テクスチャの透明部分を捨てて描きます。\n"
    "    ★【影も同じ形に抜けます】。葉の影が板の影にならないよう、影とカメラの深度パスでも\n"
    "      同じテクスチャ・同じしきい値で抜いています。\n"
    "  ・半透明(Blend): 不透明を全部描いた後に、カメラから遠い順で描きます。\n"
    "    深度を書かず影も落とさないので、ガラスが黒い板の影を落とすことはありません。\n"
    "  インスペクターの Material に「透明」欄が増えました(継承 / 不透明 / Mask / Blend +\n"
    "  しきい値 + 不透明度)。既定は【継承】＝モデルに従うので、既存のシーンは 1 ピクセルも\n"
    "  変わりません。不透明度を下げるだけでもガラスになります。\n"
    "\n"
    "■【新機能】アセットのホットリロード(エディタを再起動しない)\n"
    "  テクスチャもモデルも、一度読むとプロセスを閉じるまでキャッシュされていたため、\n"
    "  Blender や画像ソフトから同じ場所へ書き出し直しても絵が変わりませんでした。\n"
    "  更新されたものだけを読み直せるようにしました(シーンは開き直しません。\n"
    "  エンティティ・位置・選択状態はそのままで、参照先だけ新しい実体に差し替わります)。\n"
    "\n"
    "■ 被写界深度(DoF)を作り直しました\n"
    "  深度の線形化が符号ごと壊れていて【画面全体がボケる】状態でした。直したうえで、\n"
    "  ピントを「絞り(F 値)と焦点距離」で決められるようにし、任意のエンティティに\n"
    "  合焦させ続けられるようにしました。\n"
    "\n"
    "■ 遠くの金属や塗装がザラつくのを抑えました(法線マップのラフネスへの焼き戻し)\n"
    "  縮小した法線マップの暴れを、そのミップのラフネスへ変換して吸収します。\n"
    "  カメラが動くたびにチラチラしていた金属のハイライトが落ち着きます。\n"
    "\n"
    "■ その他\n"
    "  ・テクスチャを読み直すたびに圧縮キャッシュ(.texcache)を二重に作っていたのを修正\n"
    "  ・モデルのサブメッシュに元の名前とマテリアル名を持たせました(どの部品か分かります)\n"
    "  ・リファレンスサイトがスマートフォンで横に溢れないようにしました\n";
} // namespace dx12e
