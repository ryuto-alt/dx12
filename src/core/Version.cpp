#include "core/Version.h"

// バージョン定数の唯一の実体。宣言・注意書きは Version.h を参照。
namespace dx12e
{
const char* const kEngineVersion = "1.12.8";

const char* const kUpdateRepoOwner = "ryuto-alt";
const char* const kUpdateRepoName  = "dx12";

const char* const kWhatsNewTitle = "DX12 Engine v1.12.8 の更新内容";

const char* const kWhatsNewBody =
    "v1.12.8: 当たり判定の表示を絞れるようにしました\n"
    "\n"
    "■【改善】当たり判定が全部出て読めなかったのを直しました\n"
    "  前のバージョンで当たり判定の表示を直しましたが、実際のシーンで ON にすると\n"
    "  壁と床の線で画面が埋まって何も読めませんでした。調べたいのは大抵 1 個の\n"
    "  オブジェクトなのに、無関係な線が何百本も邪魔をしている状態でした。\n"
    "\n"
    "  絞り込みを 2 種類足しました。\n"
    "\n"
    "■ 表示範囲（どこを出すか）\n"
    "    すべて              … 従来どおり\n"
    "    選択中のみ（子も）  … ★一番使いやすい。Hierarchy で選んだものとその子だけ\n"
    "    カメラの近くだけ    … 距離をメートルで指定\n"
    "\n"
    "  「このオブジェクトがすり抜ける」を調べるときは、Hierarchy で選ぶだけで\n"
    "  それ以外の線が消えます。\n"
    "\n"
    "■ 種類ごとの表示（何を出すか）\n"
    "  設定に出ている色の凡例が、そのままチェックボックスになりました。\n"
    "  色を見て「これが邪魔」と思ったその場で消せます。\n"
    "\n"
    "    ・動く (Dynamic) / 動かない (Static) / Kinematic\n"
    "    ・CharacterController / トリガー / ★当たらない\n"
    "\n"
    "  ★線が埋まる原因はたいてい「動かない (Static)」（床と壁）です。\n"
    "    まずこれを切ってみてください。劇的に減ります。\n"
    "\n"
    "■ 既定は変えていません\n"
    "  最初から Static を隠すことも考えましたが、今度は「なぜ壁の判定が出ないのか」に\n"
    "  なって同じだけ分かりにくくなります。絞る操作をその場に置くことで解きました。\n"
    "\n"
    "■ 既存プロジェクトへの影響\n"
    "  ありません。当たり判定そのものの計算は変わっていません。\n";
} // namespace dx12e
