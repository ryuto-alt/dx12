#include "core/Version.h"

// バージョン定数の唯一の実体。宣言・注意書きは Version.h を参照。
namespace dx12e
{
const char* const kEngineVersion = "1.12.5";

const char* const kUpdateRepoOwner = "ryuto-alt";
const char* const kUpdateRepoName  = "dx12";

const char* const kWhatsNewTitle = "DX12 Engine v1.12.5 の更新内容";

const char* const kWhatsNewBody =
    "v1.12.5: 画面シェーダーが見つからないときにエラーが数百行あふれる問題を直しました\n"
    "\n"
    "■【修正】エラーで画面が埋まって作業できなくなる問題\n"
    "  カメラに割り当てた「画面シェーダー」の .hlsl が見つからない、またはコンパイル\n"
    "  できない状態になると、同じエラーが 1 フレームに 1 行ずつ積み上がり、\n"
    "  数百行でコンソールが埋まっていました。ほかのエラーが読めなくなるため、\n"
    "  実質そのまま作業が続けられない状態になります。\n"
    "\n"
    "  よく踏むのは【エディタを開いたまま Git のブランチを切り替えて .hlsl が消えた】ときです。\n"
    "\n"
    "  これからは理由が 1 回だけ出て、そこで止まります。\n"
    "  ・メッシュとスプライトのカスタムシェーダーは元からこの動きでした。\n"
    "    画面シェーダーだけ揃っていなかったのを合わせています。\n"
    "  ・.hlsl を直して保存する、消えていたファイルが戻る、といった場合は\n"
    "    これまでどおり自動で作り直されます（あきらめっぱなしにはなりません）。\n"
    "\n"
    "■ 直し方の目安\n"
    "  エラーが出たら、カメラの Inspector「画面シェーダー」欄の下に理由が赤枠で出ます。\n"
    "  ・パスは assets/shaders/ からの相対で指定します\n"
    "  ・VSMain と PSMain の両方が必要です\n"
    "  ・「書式を見る」ボタンで、宣言してよいレジスタの一覧が読めます\n"
    "\n"
    "■ 既存シーンへの影響\n"
    "  ありません。動いていたシェーダーの見た目や設定は一切変わりません。\n";
} // namespace dx12e
