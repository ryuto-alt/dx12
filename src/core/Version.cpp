#include "core/Version.h"

// バージョン定数の唯一の実体。宣言・注意書きは Version.h を参照。
namespace dx12e
{
const char* const kEngineVersion = "1.12.1";

const char* const kUpdateRepoOwner = "ryuto-alt";
const char* const kUpdateRepoName  = "dx12";

const char* const kWhatsNewTitle = "DX12 Engine v1.12.1 の更新内容";

const char* const kWhatsNewBody =
    "v1.12.1: カスタムシェーダーのエラーが「読んで直せる」ようになりました\n"
    "\n"
    "■【修正】「PSO の生成に失敗しました」だけで終わっていた問題\n"
    "  自作シェーダーが効かないとき、これまではログに一行出るだけで、\n"
    "  何が悪いのか・何なら書いてよいのかがどこにも書いてありませんでした。\n"
    "  今は失敗した理由を自力で特定して、こう出します:\n"
    "\n"
    "    ■ 原因: このパスに無いレジスタを宣言しています\n"
    "        NG  b1  \"MyExtra\"\n"
    "        NG  t2  \"gNoise\"\n"
    "\n"
    "  シェーダーのリフレクションと、そのパスのルートシグネチャを突き合わせて\n"
    "  照合しているので、D3D12 のデバッグレイヤーが無い配布版でも同じ説明が出ます。\n"
    "  （使っていなくても、宣言しただけでレジスタは要求されます。そこが一番の罠でした）\n"
    "\n"
    "■【新機能】「宣言してよいレジスタ」の一覧＝書式をその場で読めます\n"
    "  エラーの下に、そのシェーダーで使えるレジスタの一覧が並びます。\n"
    "  例（画面シェーダー）: b0 = 定数、t0 = 画面カラー、t1 = 深度、s0/s1 = サンプラー。\n"
    "  エラーが無いときも Inspector の「書式を見る」ボタンからいつでも開けます。\n"
    "  一覧はルートシグネチャの実体から作っているので、説明が実装とズレません。\n"
    "  メッシュ用 / スプライト用 / 画面シェーダーの 3 種それぞれに対応しています。\n"
    "\n"
    "■【改善】エラーが出る場所を増やしました\n"
    "  ・Inspector … シェーダーを割り当てたその欄に、赤いボックスで全文が出ます\n"
    "  ・コンソール … 行を右クリックで全文コピー\n"
    "  ・超詳細診断 … 「コンパイルは通ったが PSO で落ちる」を拾えていませんでした\n"
    "    （＝異常なしと報告してしまっていた）。ここも名指しします\n"
    "  ・MCP … dx12_create_shader / dx12_read_shader が issue として同じ文面を返します\n"
    "\n"
    "■【新機能】コンソールのエラーをクリックすると、その場所へ飛びます\n"
    "  そのシェーダーを使っているエンティティを選び直して、Inspector の\n"
    "  該当セクション（Shader / 画面シェーダー）を開き、そこまでスクロールします。\n"
    "\n"
    "■【改善】エラー文と書式はドラッグ選択してコピーできます\n"
    "  Ctrl+C での部分コピーと、「全文をコピー」ボタンの両方が使えます。\n"
    "\n"
    "■【改善】コンパイルエラーも同じ場所に出ます\n"
    "  HLSL がコンパイルできていない場合は、DXC の行番号付きエラーを最優先で表示します。\n"
    "  「シェーダーが見つからない」ときも、原因の候補を並べるようにしました。\n"
    "\n"
    "■ 既存シーンへの影響\n"
    "  ありません。描画結果は v1.12.0 と同じです（表示される情報が増えただけ）。\n";
} // namespace dx12e
