#include "core/Version.h"

// バージョン定数の唯一の実体。宣言・注意書きは Version.h を参照。
namespace dx12e
{
const char* const kEngineVersion = "1.12.2";

const char* const kUpdateRepoOwner = "ryuto-alt";
const char* const kUpdateRepoName  = "dx12";

const char* const kWhatsNewTitle = "DX12 Engine v1.12.2 の更新内容";

const char* const kWhatsNewBody =
    "v1.12.2: 見た目プリセットを複数選べるようにし、サムネイルを付けました\n"
    "\n"
    "■【新機能】プリセットを重ねられるようになりました\n"
    "  今までは 1 個押すと丸ごと置き換わるだけで、シネマ + グリッチ のような\n"
    "  組み合わせが作れませんでした（後から押した方に上書きされていた）。\n"
    "  今はクリックで ON/OFF するトグルで、いくつでも重ねられます。\n"
    "  ・重なる順番は【表の並び順】。押した順ではないので、同じ組み合わせなら\n"
    "    必ず同じ絵になります（同じ項目を触るプリセット同士は後ろが勝ちます）。\n"
    "  ・選択中のタイルには 1, 2, 3 … と重なる順番が出ます。\n"
    "  ・外すと、そのプリセットが入れた値だけがきれいに消えます\n"
    "    （毎回「既定へ戻す → 選ばれたものを順に適用」で作り直しているため）。\n"
    "\n"
    "■【新機能】プリセットにサムネイルが付きました\n"
    "  文字だけのボタンだったので、押してみるまでどんな絵になるか分かりませんでした。\n"
    "  空 / 地面 / 建物 / 太陽の小さな見本画に、そのプリセットの設定を当てた絵を出します。\n"
    "  走査線・ドット・粒子・周辺減光・魚眼の丸・グリッチの帯などがそのまま見えます。\n"
    "  ★プリセットの数値から描いているので、値を直せばサムネイルも一緒に変わります。\n"
    "\n"
    "■【改善】選択中の組み合わせと、戻す手段が分かりやすくなりました\n"
    "  ・「選択中: シネマ + ホラー」のように現在の組み合わせを表示します\n"
    "  ・「すべて外す（素の絵）」… 味付けだけ落とす（露出・DoF・ブルーム品質は残る）\n"
    "  ・「↩ 選ぶ前に戻す」… プリセットを 1 つ目に選ぶ直前の状態へ完全に戻す\n"
    "  ・シーンを開き直すと選択状態はリセットされます\n"
    "    （別のシーンで取った控えを「戻す」で流し込んでしまう事故を防ぐため）\n"
    "\n"
    "■【修正】説明文が窓の右端で切れていました\n"
    "  Post Process 窓を右へドッキングすると幅が狭く、プリセットの説明が読めません\n"
    "  でした。折り返すようにしました。\n"
    "\n"
    "■ 既存シーンへの影響\n"
    "  ありません。プリセットが入れる値そのものは v1.12.0 から変えていません。\n";
} // namespace dx12e
