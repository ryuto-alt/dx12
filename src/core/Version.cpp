#include "core/Version.h"

// バージョン定数の唯一の実体。宣言・注意書きは Version.h を参照。
namespace dx12e
{
const char* const kEngineVersion = "1.12.6";

const char* const kUpdateRepoOwner = "ryuto-alt";
const char* const kUpdateRepoName  = "dx12";

const char* const kWhatsNewTitle = "DX12 Engine v1.12.6 の更新内容";

const char* const kWhatsNewBody =
    "v1.12.6: ブランチのマージができるようになり、Git 窓が見て分かるようになりました\n"
    "\n"
    "■【新機能】ブランチをマージできるようになりました\n"
    "  これまでブランチ操作は 切替 / 作成 / 名前変更 / 削除 だけで、マージがありませんでした。\n"
    "  コンフリクトを解消する画面はあったのに、そこへ入る道がプルしかなかったため、\n"
    "  「機能ブランチを取り込む」という一番普通の操作がエディタからできませんでした。\n"
    "\n"
    "  Git 窓のマージ ボタン（またはブランチを右クリック）から取り込めます。\n"
    "  実行する前に、こちらが分かるようにしてあります:\n"
    "    ・取り込まれるコミット数（0 なら「すでに最新です」と出ます）\n"
    "    ・未コミットの変更が残っているという警告（この状態だと git が拒否します）\n"
    "    ・マージコミットを必ず作るかどうか（既定 ON。どこから取り込んだかが履歴に残ります）\n"
    "\n"
    "  コンフリクトしたら、これまでどおり一覧が出て「自分優先 / 相手優先 / 開く」で\n"
    "  解消できます。解消してコミットすればマージ完了です。\n"
    "\n"
    "■【新機能】「マージを中止」を追加しました\n"
    "  コンフリクトを解消しきれないときに、始める前の状態へ戻せます。\n"
    "  これまではエディタだけでは抜けられず、コマンドラインを開く必要がありました。\n"
    "\n"
    "■【改善】Git 窓の操作がアイコンになりました\n"
    "  「更新」「フェッチ」という文字ボタンと、上下の矢印ボタンが混ざっていて、\n"
    "  どれが送信でどれが受信なのか押すまで分かりませんでした。\n"
    "  色と形が違うアイコンに変えたので、狭い場所に置いても並びが崩れず、\n"
    "  マウスを乗せれば「プッシュ — 手元のコミットをリモートへ送る（送信）」のように\n"
    "  名前と意味が出ます。\n"
    "\n"
    "  未送信/未取得の件数は、0 のときは表示しないようにしました。\n"
    "  常に「↑0 ↓0」が出ていると、本当に送るものがある時に気づけないためです。\n"
    "\n"
    "■【新機能】AI から Git を操作できるようになりました（MCP）\n"
    "  状態の確認・ブランチ一覧・切替・マージ・コミット・プッシュ・プル・フェッチを\n"
    "  AI から実行できます。エディタの Git 窓と同じ処理を呼ぶので、手で操作したときと\n"
    "  結果が食い違いません。\n"
    "  ★プッシュは外向きの操作なので、頼まれていない限り AI が勝手に実行しないよう\n"
    "    ツールの説明に明記してあります。\n"
    "\n"
    "■ 既存プロジェクトへの影響\n"
    "  ありません。今までの操作の位置と結果は変わっていません。\n";
} // namespace dx12e
