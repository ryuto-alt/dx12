#include "core/Version.h"

// バージョン定数の唯一の実体。宣言・注意書きは Version.h を参照。
namespace dx12e
{
const char* const kEngineVersion = "1.5.1";

const char* const kUpdateRepoOwner = "ryuto-alt";
const char* const kUpdateRepoName  = "dx12";

const char* const kWhatsNewTitle = "DX12 Engine v1.5.1 の更新内容";
const char* const kWhatsNewBody =
    "v1.5.1: エンジン診断の誤検知を修正\n"
    "\n"
    "・[すべてのツール窓を開いて描画] が「Post Process パラメータ : 中身が空」\n"
    "  で失敗していたのを修正。ポストプロセスを何も有効にしていないシーンでは\n"
    "  この窓は案内文だけを出すのが正常で、不具合ではありませんでした。\n"
    "\n"
    "v1.5.0 の内容(本バージョンに含まれます):\n"
    "エンジン診断を大幅に強化（検査項目 6 → 22）\n"
    "\n"
    "[ツール > エンジン診断] が、エディタのほぼ全機能を自動で触って\n"
    "クラッシュ・フリーズ・エラーを探すようになりました。\n"
    "\n"
    "・検査対象を追加: UIエディタ / パーティクルエディタ / マテリアル /\n"
    "  ビルド(実際に書き出して成果物を確認) / 再生(Play→Stop) /\n"
    "  コンソール(Lua実行) / アセットブラウザ / 全ツール窓 / 連打ストレス\n"
    "・結果画面を作り直し: グループ別の一覧、グループ単位の再検査、\n"
    "  進捗バー、失敗理由をその場に表示、[失敗だけ再検査]\n"
    "・検査前にシーンを退避し、終わったら自動で元に戻します\n"
    "  (検査でシーンが汚れません)\n"
    "・クラッシュレポート(dx12_crash.log)に「直前の操作」を最大16件記録。\n"
    "  スタックトレースだけでは分からない“どの操作で落ちたか”が残ります\n"
    "・フリーズ検出を追加。固まった検査は打ち切って失敗として記録します\n"
    "・落ちなくても検査中にエラーログが出ていれば、その項目に表示します\n"
    "\n"
    "v1.4.6 の内容(本バージョンに含まれます):\n"
    "・エンジン診断パネルの追加、エディタ終了時のクラッシュ修正\n"
    "\n"
    "※ v1.4.2 以前をお使いの場合、自動更新が失敗することがあります。\n"
    "   その場合は GitHub Releases から最新 zip を手動で入れ直してください。\n";
} // namespace dx12e
